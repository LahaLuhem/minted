{
  "_comment": "Single source of truth for container-based lint checks, shared by .github/workflows/repo.yml (CI dynamic matrix) and scripts/release.sh (release preflight). Add a linter = add one { name, cmd } entry to `checks`. Bump the image tag here, in one place. https://github.com/LahaLuhem/linterpol",
  "image": "ghcr.io/lahaluhem/linterpol:latest",
  "checks": [
    { "name": "ShellCheck", "cmd": "shellcheck scripts/*.sh" },
    { "name": "actionlint", "cmd": "actionlint" }
  ]
}
