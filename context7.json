{
  "url": "https://context7.com/lahaluhem/minted",
  "public_key": "pk_zRQqaALefsVNkJ7uS6fVf",
  "projectTitle": "minted",
  "description": "Well-modelled Dart value types (domain primitives) for what is usually left as a raw String, int or double: email, IBAN, UUID, IP address, calendar dates and other ISO/RFC/GS1-standardised values, plus constraint types like Port and Percentage. Parse, don't validate: every instance is guaranteed well-formed.",
  "folders": [],
  "excludeFolders": [
    "build",
    "test",
    "scripts",
    ".github",
    ".idea",
    ".dart_tool",
    ".ai"
  ],
  "excludeFiles": [
    "CHANGELOG.md",
    "AGENTS.md",
    "CLAUDE.md",
    "CODESTYLE.md"
  ],
  "rules": [
    "Parse, don't validate: no type has a public constructor, so a value that exists is guaranteed well-formed, the same guarantee `Uri` gives for URLs. Build one through the type's own door, never a constructor.",
    "`parse` does NOT throw. It returns a `ParseOutcome`: either the value, or a typed failure from that type's own vocabulary (`IbanFailure`, `DateFailure`, ...), so you can `switch` on the cause or read `.reasonOrNull` for a form-field message. `tryParse` is the same check returning `null`. The doors that DO throw `MintedFormatException` (a `FormatException`) are the assembly factories you call when claiming the parts are already valid (`fromComponents`, `from`, `of`), and `.getOrThrow()` on an outcome.",
    "Not every type parses text. Constraint types (`Port`, `Uint`, `NaturalNumber`, `Uint2` through `Uint32`, `Percentage`) take a `tryFrom` door over a number (`int` on the integer types, `num` on `Percentage`) and deliberately have no `parse` / `tryParse`, because no standard defines a text form for a bare number. Classifications (`Weekday`, `UuidVariant`, `PhoneNumberType`, `CardScheme`, `PortRange`, `Iso8601DurationComponent`) are enums a value type hands back, so they have no parse door either. `Month` is NOT one of these: it is a value type and does parse.",
    "Single-primitive types are zero-cost extension types over that primitive: `Email` over String, `Port` over int, `Percentage` over double. Types with parts worth naming (`GeoCoordinate`, `Cidr`, `PaymentCardNumber`) are immutable classes. Both give value equality, hashCode, and a canonical form read back from `.value`.",
    "Types normalise on parse, so equality is canonical: `Iban.parse('gb82 west ...') == Iban.parse('GB82WEST...')`. Compare parsed values, never the raw strings you parsed them from.",
    "Check-digit types validate the real standard, not a regex shape: IBAN mod-97, card numbers Luhn, ISBN/EAN/ISSN check digits. A value that parses has passed its checksum.",
    "`Percentage` names the unit that a raw double cannot: `Percentage.tryFrom(15)` and `Percentage.tryFromFraction(0.15)` are the same proportion, `.value` holds the percent, `.fraction` the fraction, and `.of(200)` applies it. It is unbounded on purpose (250% growth and -12% churn are real), so finiteness is its only invariant.",
    "Pure Dart with minimal dependencies, so it runs in CLI, server, web and Flutter. Opinionated integrations (JSON, fpdart, Hive, Flutter form validators) are opt-in companion packages, never forced on core consumers."
  ],
  "previousVersions": []
}
